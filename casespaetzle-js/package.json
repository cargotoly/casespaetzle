{
  "name": "casespaetzle",
  "version": "1.0.2",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "(cd ../casespaetzle; wasm-pack build --out-dir ../casespaetzle-js/pkg --target nodejs) && node build.js",
    "test": "node test.js",
    "prepublish": "npm run build"
  },
  "keywords": [
    "string",
    "cases",
    "util"
  ],
  "author": "Anatoly Weinstein",
  "license": "Apache-2.0",
  "description": "This package extends the string prototype with case conversion methods."
}
